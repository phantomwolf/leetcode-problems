问题：给定一个数组，求里面唯一一个出现奇数次的元素
例如：输入[1, 2, 3, 2, 3, 1, 3]，输出3。


方法1：统计
时间：O(n)
空间：O(n)
统计所有元素出现的次数，存如散列表/二叉查找树，最后找出出现次数为奇数的。


方法2：异或XOR
时间：O(n)
空间：O(1)
我们知道，相同的位异或结果为0；不同的位异或结果为1。且不论x、y、z的值为什么，x ^ y ^ z ^ y = x ^ z。同样一个操作数，XOR两次必定会抵消，这与运算的顺序无关。
本题中，我们可以利用异或的性质，让所有元素进行异或，将出现偶数次的元素抵消，最后结果就是出现奇数次的元素。
