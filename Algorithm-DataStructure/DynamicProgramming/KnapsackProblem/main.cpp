#include <iostream>
#include <vector>

using namespace std;

// pair: weight, value
int knapsack(int capacity, vector<pair<int, int>> wv)
{
    const int len = wv.size();
    int d[capacity + 1] = {0};

}


int main()
{
    return 0;
}
