#include <stdio.h>
#include <stdlib.h>


// lens[]: 每个单词的长度
// n: lens的长度
// M: 每行最大长度
int solveWordWrap(int lens[], int n, int M)
{
    int sum[n + 1];
    int lc[n + 1][n + 1];

    sum[0] = 0;
    for (int i = 0; i <= n; ++i) {
    }

    for (int i = 1; i <= n; ++i) {
        for (int j = 1; j <= n; ++j) {
        }
    }
}


int main(int argc, char *argv[])
{
    return 0;
}
