#include <iostream>
#include <vector>
#include <stack>

using namespace std;



int solution(vector<vector<int>> matrix, vector<int> &longest_path)
{
    int n = matrix.size();
    int d[n][n] = {0};

    for (int i = 0; i < n; ++i) {
        for (int j = 0; j < n; ++j) {
        }
    }
}

int main(int argc, char *argv[])
{
    vector<vector<int>> d(n, vector<int>(n, 0));
    return 0;
}
