#include <iostream>
#include <vector>
using namespace std;

class Solution {
public:
    int strStr(string haystack, string needle) {
        vector<int> lps(needle.size());
        // Calculate lps
        for (int i=0, len=0; i < needle.size();) {
        }
    }
};

int main(int argc, char *argv[]) {
}
