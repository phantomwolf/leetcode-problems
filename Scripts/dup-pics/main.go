// Print all the duplicated pictures.
// Note: My pictures are named as their sha1 hash values
package main

import (
//    "fmt"
    "os"
    "lib/singleton"
)

func handle(path string, info os.FileInfo, err error) error {
    files["baka"] = "linux"
    return err
}

func main() {
}
